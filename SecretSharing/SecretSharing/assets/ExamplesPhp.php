<?php
    /* CRUD OPERATION EXAMPLES FOR USER*/

    /*Código para borrar a un usuario: Works!
        $result = $DBConnection->borraUsuario($userForm);
    */

    /*Código para actualizar a un usuario: Works!
        $result = $DBConnection->actualizaUsuario($userForm);
    */

    /*Código para insertar a un usuario: Works!
        $DBConnection->insertaUsuario($userForm);
    */
    
    /*Código para consultar a un usuario: Works!
        $user = new Usuario();
        $user = $DBConnection->consultaUsuario($userForm);
        if($user == null){
            echo "El usuario no existe";
        }else{
            echo "Usuario existente redirigir a página principal \n e iniciar sesion";}
        }
    */
?>